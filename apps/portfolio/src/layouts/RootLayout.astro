---
import "../styles/style.css";
import { type Metadata, mergeMetadata } from "../types/Metadata";

import Footer from "./Footer.astro";

interface Props {
    path?: string;
    metadata?: Metadata;
}

const defaultMetadata: Metadata = {
    title: "Library • Farhan Ali ",
    description:
        "Farhan Ali is a software enginner building deep softwares with simple and clean look",
    metadataBase: "https://farhanali.org",
    authors: [{ name: "Farhan Ali" }],
    robots: { index: true, follow: true },
    themeColor: "#F0EEED",
    colorScheme: "dark light",
    alternates: { canonical: "https://farhanali.org" },
    manifest: "/site.webmanifest",
    openGraph: {
        type: "website",
        url: "https://farhanali.org",
        title: "Farhan Ali • Software Engineer based in Pakistan",
        description:
            "Farhan Ali is a software enginner building deep softwares with simple and clean look",
        images: [{ url: "https://farhanali.org/images/social-preview.png" }],
    },
    twitter: {
        card: "summary_large_image",
        site: "@farhanali.org",
        title: "Farhan Ali • Software Engineer based in Pakistan",
        description:
            "Farhan Ali is a software enginner building deep softwares with simple and clean look",
        images: ["https://farhanali.org/images/social-preview.png"],
    },
};

const { path = "/", metadata: propsMetadata } = Astro.props;

// Compute field-level merging where child overrides parent
const metadata = mergeMetadata(defaultMetadata, propsMetadata);

// Logical flags to confirm a block contains defined content before generating elements
const hasOpenGraph = !!(
    metadata.openGraph?.type ||
    metadata.openGraph?.url ||
    metadata.openGraph?.title ||
    metadata.openGraph?.description ||
    metadata.openGraph?.images
);
const hasTwitter = !!(
    metadata.twitter?.card ||
    metadata.twitter?.site ||
    metadata.twitter?.title ||
    metadata.twitter?.description ||
    metadata.twitter?.images
);

// Fallback logic for image string structures
const ogImageUrl =
    typeof metadata.openGraph?.images?.[0] === "string"
        ? metadata.openGraph.images[0]
        : (metadata.openGraph?.images?.[0] as any)?.url;

const twitterImageUrl =
    typeof metadata.twitter?.images?.[0] === "string"
        ? metadata.twitter.images[0]
        : ((metadata.twitter?.images?.[0] as any)?.url ?? ogImageUrl);
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        {
            metadata.title && (
                <title>
                    {typeof metadata.title === "string"
                        ? metadata.title
                        : metadata.title?.default}
                </title>
            )
        }
        {
            metadata.description && (
                <meta name="description" content={metadata.description} />
            )
        }

        {
            /* Fixed: removed /public prefix so assets load from root in built environments */
        }
        <link
            rel="icon"
            type="image/png"
            sizes="32x32"
            href="/images/favicon.png"
        />
        <link
            rel="apple-touch-icon"
            sizes="180x180"
            href="/images/favicon.png"
        />

        {
            metadata.alternates?.canonical && (
                <link rel="canonical" href={metadata.alternates.canonical} />
            )
        }
        {
            metadata.authors?.[0]?.name && (
                <meta name="author" content={metadata.authors[0].name} />
            )
        }

        <meta
            name="robots"
            content={`${metadata.robots?.index !== false ? "index" : "noindex"}, ${metadata.robots?.follow !== false ? "follow" : "nofollow"}`}
        />
        {
            metadata.themeColor && (
                <meta
                    name="theme-color"
                    content={
                        typeof metadata.themeColor === "string"
                            ? metadata.themeColor
                            : "#F0EEED"
                    }
                />
            )
        }
        {
            metadata.colorScheme && (
                <meta name="color-scheme" content={metadata.colorScheme} />
            )
        }

        {
            /* Conditional OpenGraph wrapper blocks tailored to field metadata definition */
        }
        {
            hasOpenGraph && (
                <>
                    {metadata.openGraph?.type && (
                        <meta
                            property="og:type"
                            content={metadata.openGraph.type}
                        />
                    )}
                    {metadata.openGraph?.url && (
                        <meta
                            property="og:url"
                            content={metadata.openGraph.url}
                        />
                    )}
                    {metadata.openGraph?.title && (
                        <meta
                            property="og:title"
                            content={metadata.openGraph.title}
                        />
                    )}
                    {metadata.openGraph?.description && (
                        <meta
                            property="og:description"
                            content={metadata.openGraph.description}
                        />
                    )}
                    {ogImageUrl && (
                        <meta property="og:image" content={ogImageUrl} />
                    )}
                </>
            )
        }

        {
            /* Conditional Twitter wrapper blocks tailored to field metadata definition */
        }
        {
            hasTwitter && (
                <>
                    {metadata.twitter?.card && (
                        <meta
                            name="twitter:card"
                            content={metadata.twitter.card}
                        />
                    )}
                    {metadata.twitter?.site && (
                        <meta
                            name="twitter:site"
                            content={metadata.twitter.site}
                        />
                    )}
                    {metadata.twitter?.title && (
                        <meta
                            name="twitter:title"
                            content={metadata.twitter.title}
                        />
                    )}
                    {metadata.twitter?.description && (
                        <meta
                            name="twitter:description"
                            content={metadata.twitter.description}
                        />
                    )}
                    {twitterImageUrl && (
                        <meta name="twitter:image" content={twitterImageUrl} />
                    )}
                </>
            )
        }

        {metadata.manifest && <link rel="manifest" href={metadata.manifest} />}

        <script type="application/ld+json">
            {
                "@context": "https://schema.org/",
                "@type": "Person",
                "name": "Farhan Ali",
                "jobTitle": "Software Engineer",
                "url": "https://farhanali.org",
                "description": "Software engineer specializing in high-performance systems architecture, Rust engineering, and minimal web design.",
                "publishingPrinciples": "https://farhanali.org/work/",
                "sameAs": [
                    "https://github.com/Farhan-Ali-Rajpoot",
                    "https://github.com/Farhan-Ali-Rajpoot/omnyx",
                    "https://github.com/Farhan-Ali-Rajpoot/dataseal"
                ],
                "knowsAbout": [
                    "Rust",
                    "High-Performance Systems",
                    "Linux",
                    "Systems Engineering",
                    "Hyper-Converged Infrastructure"
                ]
            }
        </script>

        <script>
            import "/src/scripts/index.js";
        </script>
    </head>

    <body id="view-container">
        <!-- Page Transition Container -->
        <div
            class="pt-container"
            style="height: 100vh; width: 100vw; display: flex; position: fixed; top: 0; left: 0; z-index: 20; pointer-events: none; 
    "
        >
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
            <div
                class="pt-column"
                style="height: 100%; flex: 1; background-color: var(--color-green); border-right: solid 1px rgba(255, 255, 255, 0.2); transition: none !important;"
            >
            </div>
        </div>

        <!-- Menu Switch -->
        <input id="menu-switch" type="checkbox" style="display: none;" />

        <label
            for="menu-switch"
            class="bg-wrapper"
            style="position: fixed; top: 0; left: 0; inset: 0; background-color: rgba(0, 0, 0, 0.25);
            z-index: 11; pointer-events: none; opacity: 0; transition: opacity 0.8s cubic-bezier(0.075, 0.82, 0.165, 1); cursor: pointer;"
        ></label>

        <!-- Navbar -->
        <nav
            style="position: absolute; top: 0px; left: 0px; width: 100%; z-index: 10; display: flex; justify-content: space-between; align-items: center;
                 padding: calc(var(--sfu)*1.75);"
        >
            <a
                href="/"
                style="display: flex; align-items: end; font-weight: 300; gap: calc(var(--sfu)*.25);"
            >
                <span
                    style="margin: 0; font-size: calc(var(--sfu)*1.25); font-weight: 400; letter-spacing: calc(var(--sfu)*-0.01);"
                >
                    Farhan Ali
                </span>
                <div
                    style="background-color: var(--color-text); height: calc(var(--sfu)*.2); width: calc(var(--sfu)*.2); border-radius: 50%;
                    transform: translateY(-125%);"
                >
                </div>
            </a>

            <div
                id="nav-links"
                style="width: fit-content; display: flex; gap: calc(var(--sfu)*4)"
            >
                <div
                    style="display: flex; align-items: center; gap: calc(var(--sfu)*2.25)"
                >
                    <a
                        data-active={path == "/" ? true : null}
                        href="/"
                        class="line-reveal-trigger"
                        style="position: relative;"
                    >
                        Home
                        <div
                            class="line-to-reveal"
                            style="position: absolute; bottom: -10%; left: 0; display: block; width: 100%; height: 1px; background-color: var(--color-text);
                         transition: transform .8s cubic-bezier(0.075, 0.82, 0.165, 1) .3s; transform: scaleX(0);"
                        >
                        </div>
                    </a>

                    <a
                        href="/work"
                        data-active={path == "/work" ? true : null}
                        class="line-reveal-trigger"
                        style="position: relative;"
                    >
                        Work
                        <div
                            class="line-to-reveal"
                            style="position: absolute; bottom: -10%; left: 0; display: block; width: 100%; height: 1px; background-color: var(--color-text);
                         transition: transform .8s cubic-bezier(0.075, 0.82, 0.165, 1) .3s; transform: scaleX(0);"
                        >
                        </div>
                    </a>

                    <a
                        href="/about"
                        data-active={path == "/about" ? true : null}
                        class="line-reveal-trigger"
                        style="position: relative;"
                    >
                        About
                        <div
                            class="line-to-reveal"
                            style="position: absolute; bottom: -10%; left: 0; display: block; width: 100%; height: 1px; background-color: var(--color-text);
                         transition: transform .8s cubic-bezier(0.075, 0.82, 0.165, 1) .3s; transform: scaleX(0);"
                        >
                        </div>
                    </a>
                </div>

                <a
                    data-magnetic
                    href="/contact/"
                    class="contact-btn roll-trigger"
                    style="padding: calc(var(--sfu)*.4) calc(var(--sfu)*1.75) calc(var(--sfu)*.4) calc(var(--sfu)*.4);
                    border-radius: 5em; background-color: var(--color-bg-surface-dark); color: var(--color-text-light); display: flex; align-items: center;
                    gap: calc(var(--sfu)*.75); text-decoration: none;"
                >
                    <div style="display: flex; align-items: center;">
                        <div
                            style="height: calc(var(--sfu)*2.4); width: calc(var(--sfu)*2.4); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary);
                            display: flex; align-items: center; justify-content: center;"
                        >
                            <svg
                                width="16"
                                height="16"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                            >
                                <path
                                    d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"
                                >
                                </path>
                            </svg>
                        </div>

                        <div
                            style="height: calc(var(--sfu)*2.8); width: calc(var(--sfu)*2.8); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary);
                            display: flex; align-items: center; justify-content: center; margin-left: calc(var(--sfu)*-.8); border: calc(var(--sfu)*.2) solid var(--color-bg-surface-dark);"
                        >
                            <svg
                                width="16"
                                height="16"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2"
                                stroke-linecap="round"
                                stroke-linejoin="round"
                            >
                                <path
                                    d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                                >
                                </path>
                                <polyline points="22,6 12,13 2,6"></polyline>
                            </svg>
                        </div>
                    </div>

                    <div
                        data-magnetic-inner
                        style="position: relative; overflow: hidden;"
                    >
                        <p
                            class="text-roll-1"
                            style="transition: transform .7s cubic-bezier(0.075, 0.82, 0.165, 1);"
                        >
                            Contact
                        </p>
                        <p
                            class="text-roll-2"
                            style="transition: transform .7s cubic-bezier(0.075, 0.82, 0.165, 1); position: absolute; 
                                top: 0; left: 0; transform: translateY(100%);"
                        >
                            Contact
                        </p>
                    </div>
                </a>
            </div>
        </nav>

        <!-- Menu -->

        <div
            class="hamburger-icon-wrapper"
            style="pointer-events: none; position: fixed; top: calc(var(--sfu)*2); right: calc(var(--sfu)*2); z-index: 15;
        transition: all .4s cubic-bezier(0.075, 0.82, 0.165, 1);"
        >
            <label
                data-magnetic
                for="menu-switch"
                class="hamburger-icon"
                style="width: calc(var(--sfu)*4); height: calc(var(--sfu)*4); border-radius: 50%;
                border: calc(var(--sfu)*.125) solid var(--color-bg-surface-dark-secondary);
            background-color: var(--color-bg-surface-dark); cursor: pointer; display: flex; justify-content: center; align-items: center; overflow: hidden;
            position: relative; transition: scale .5s cubic-bezier(0.075, 0.82, 0.165, 1);
        "
            >
                <div
                    data-magnetic-inner
                    style="z-index: 2; display: flex; flex-direction: column; gap: calc(var(--sfu)*.375);"
                >
                    <div
                        class="line-top"
                        style="width: calc(var(--sfu)*1.35); height: calc(var(--sfu)*.125); background-color: var(--color-bg);
                 border-radius: calc(var(--sfu)*.125); transition: transform 0.3s ease;"
                    >
                    </div>
                    <div
                        class="line-bottom"
                        style="width: calc(var(--sfu)*1.35); height: calc(var(--sfu)*.125); background-color: var(--color-bg);
                 border-radius: calc(var(--sfu)*.125); transition: transform 0.3s ease;"
                    >
                    </div>
                </div>
                <div
                    class="bg-active"
                    style="position: absolute; inset: 0; z-index: 1; background-color: var(--color-green); border-radius: 50%;
                        transform: translateY(100%); transition: transform 0.4s cubic-bezier(0.76, 0, 0.24, 1);"
                >
                </div>
            </label>
        </div>

        <div
            id="menu"
            style="position: fixed; width: calc(var(--sfu)*2); height: calc(var(--sfu)*2); border-radius: 50%; background-color: var(--color-bg-surface-dark);
            color: var(--color-text-light); display: flex; flex-direction: column; justify-content: space-between; align-items: start;  z-index: 14;
            overflow: hidden; padding: 0; top: calc(var(--sfu)*3); right: calc(var(--sfu)*3); max-width: calc(var(--sfu)*23);
            transition: all 0.5s cubic-bezier(0.76, 0, 0.24, 1), scale 0s; scale: 0; 
        "
        >
            <div style="color: var(--color-text-muted);">Menu</div>

            <div
                id="menu-links"
                style="display: flex; flex-direction: column; gap: calc(var(--sfu)*.25); font-size: calc(var(--sfu)* 2);"
            >
                <a
                    href="/"
                    data-active={path == "/" ? true : null}
                    style="display: flex; align-items: center;"
                >
                    <div
                        class="dot"
                        style="height: calc(var(--sfu)*.5); width: calc(var(--sfu)*.5); background-color: var(--color-text-light); display: inline-block; 
                border-radius: 50%; position: absolute;"
                    >
                    </div>
                    <span style="display: inline-block;">Home</span>
                </a>
                <a
                    href="/work/"
                    data-active={path == "/work" ? true : null}
                    style="display: flex; align-items: center;"
                >
                    <div
                        class="dot"
                        style="height: calc(var(--sfu)*.5); width: calc(var(--sfu)*.5); background-color: var(--color-text-light); display: inline-block; 
                border-radius: 50%; position: absolute;"
                    >
                    </div>
                    <span style="display: inline-block;">Work</span>
                </a>
                <a
                    href="/about/"
                    data-active={path == "/about" ? true : null}
                    style="display: flex; align-items: center"
                    ><span style="display: inline-block;">About</span>
                    <div
                        class="dot"
                        style="height: calc(var(--sfu)*.5); width: calc(var(--sfu)*.5); background-color: var(--color-text-light); display: inline-block; 
                border-radius: 50%; position: absolute;"
                    >
                    </div>
                </a>
                <a
                    href="/contact/"
                    data-active={path == "/contact" ? true : null}
                    style="display: flex; align-items: center;"
                    ><span style="display: inline-block;">Contact</span>
                    <div
                        class="dot"
                        style="height: calc(var(--sfu)*.5); width: calc(var(--sfu)*.5); background-color: var(--color-text-light); display: inline-block; 
                border-radius: 50%; position: absolute;"
                    >
                    </div>
                </a>
            </div>

            <div
                class="social-links"
                style="width: 100%; display: flex; flex-direction: column; justify-content: center; align-items: center; gap: calc(var(--sfu)*-0.5)"
            >
                <div
                    style="display: flex; gap: calc(var(--sfu)*.5); width: 100%; justify-content: space-evenly;"
                >
                    <a
                        data-magnetic
                        href="https://linkedin.com/in/YOUR_USERNAME"
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label="LinkedIn"
                        style="height: calc(var(--sfu)*3.5); width: calc(var(--sfu)*3.5); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary); display: flex;
                            align-items: center; justify-content: center; color: var(--color-text-light, #ffffff); text-decoration: none;
                            border: calc(var(--sfu)*.0625) solid transparent; flex-shrink: 0;"
                    >
                        <svg
                            data-magnetic-inner
                            width="22"
                            height="22"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path
                                d="M16 8a6 6 0 0 1 6 6v7h-4v-7a2 2 0 0 0-2-2 2 2 0 0 0-2 2v7h-4v-7a6 6 0 0 1 6-6z"
                            >
                            </path>
                            <rect x="2" y="9" width="4" height="12"></rect>
                            <circle cx="4" cy="4" r="2"></circle>
                        </svg>
                    </a>

                    <a
                        data-magnetic
                        href="https://x.com/FaranAli_Dev"
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label="X (Twitter)"
                        style="height: calc(var(--sfu)*3.5); width: calc(var(--sfu)*3.5); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary); display: flex;
                            align-items: center; justify-content: center; color: var(--color-text-light, #ffffff); text-decoration: none; 
                            border: calc(var(--sfu)*.0625) solid transparent; flex-shrink: 0;"
                    >
                        <svg
                            data-magnetic-inner
                            width="20"
                            height="20"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2.5"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path d="M4 4l11.733 16h4.267l-11.733 -16z"></path>
                            <path d="M4 20l6.768 -6.768m2.46 -2.46l6.772 -6.772"
                            ></path>
                        </svg>
                    </a>
                </div>

                <div
                    style="display: flex; gap: calc(var(--sfu)*.5); width: 100%; justify-content: space-between;"
                >
                    <a
                        data-magnetic
                        href="https://www.instagram.com/ali_farhan_oop"
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label="Instagram"
                        style="height: calc(var(--sfu)*3.5); width: calc(var(--sfu)*3.5); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary); 
                            display: flex; align-items: center; justify-content: center; color: var(--color-text-light, #ffffff); text-decoration: none; 
                            border: calc(var(--sfu)*.0625) solid transparent; flex-shrink: 0;"
                    >
                        <svg
                            data-magnetic-inner
                            width="22"
                            height="22"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <rect
                                x="2"
                                y="2"
                                width="20"
                                height="20"
                                rx="5"
                                ry="5"></rect>
                            <path
                                d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z"
                            ></path>
                            <line x1="17.5" y1="6.5" x2="17.51" y2="6.5"></line>
                        </svg>
                    </a>

                    <a
                        data-magnetic
                        href="https://www.tiktok.com/@duferminded"
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label="TikTok"
                        style="height: calc(var(--sfu)*3.5); width: calc(var(--sfu)*3.5); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary); 
                            display: flex; align-items: center; justify-content: center; color: var(--color-text-light, #ffffff); text-decoration: none;
                            border: calc(var(--sfu)*.0625) solid transparent; flex-shrink: 0;"
                    >
                        <svg
                            data-magnetic-inner
                            width="22"
                            height="22"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path d="M9 12a4 4 0 1 0 4 4V4a5 5 0 0 0 5 5"
                            ></path>
                        </svg>
                    </a>

                    <a
                        data-magnetic
                        href="https://www.youtube.com/@FarhanAli-l1d4q"
                        target="_blank"
                        rel="noopener noreferrer"
                        aria-label="YouTube"
                        style="height: calc(var(--sfu)*3.5); width: calc(var(--sfu)*3.5); border-radius: 50%; background-color: var(--color-bg-surface-dark-secondary); display: flex; 
                            align-items: center; justify-content: center; color: var(--color-text-light, #ffffff); text-decoration: none; 
                            border: calc(var(--sfu)*.0625) solid transparent; flex-shrink: 0;"
                    >
                        <svg
                            data-magnetic-inner
                            width="22"
                            height="22"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                            stroke-linecap="round"
                            stroke-linejoin="round"
                        >
                            <path
                                d="M22.54 6.42a2.78 2.78 0 0 0-1.95-1.96C18.88 4 12 4 12 4s-6.88 0-8.59.46a2.78 2.78 0 0 0-1.95 1.96A29 29 0 0 0 1 12a29 29 0 0 0 .46 5.58 2.78 2.78 0 0 0 1.95 1.96C5.12 20 12 20 12 20s6.88 0 8.59-.46a2.78 2.78 0 0 0 1.95-1.96A29 29 0 0 0 23 12a29 29 0 0 0-.46-5.58z"
                            >
                            </path>
                            <polygon
                                points="9.75 15.02 15.5 12 9.75 8.98 9.75 15.02"
                            ></polygon>
                        </svg>
                    </a>
                </div>
            </div>
        </div>

        <!-- Main Content -->
        <div id="swup">
            <slot />
        </div>

        <!-- Footer -->
        <Footer />
    </body>
</html>
